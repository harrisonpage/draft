<!DOCTYPE html>
<html>
<head>
    <title>{{ .Headers.title }} – Tags</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta charset="utf-8" />
    <meta name="author" content="{{ .Author }}"/>
    <meta name="copyright" content="{{ .Copyright }}"/>
    <meta name="revision" content="draft/{{ .Version }}"/>
    <meta name="updated" content="{{ .Now }}"/>
    <link rel="alternate" type="application/rss+xml" title="{{ .Headers.title }}" href="{{ .RSSLink }}"/>
    {{- if .CSSFiles }}
        {{- range .CSSFiles }}
    <link rel="stylesheet" href="{{ . }}">
        {{- end }}
    {{- end }}
    {{- if .JSFiles }}
        {{- range .JSFiles }}
    <script src="{{ . }}"></script>
        {{- end }}
    {{- end }}
    <meta name="robots" content="index,follow"/>
</head>
<body>
    <header>
        <h1>All Tags</h1>
        <p><a href="../index.html">← Back to Posts Index</a></p>
    </header>
    <main>
        <ul>
        {{ range $tag, $posts := .Tags }}
            <li>
                <h2><a href="{{ $tag.URL }}">{{ $tag.TagName }}</a></h2>
                <p>{{ len $posts }} post(s) tagged with "{{ $tag.TagName }}"</p>
            </li>
        {{ end }}
        </ul>
    </main>
    <footer>
        <a href="{{ .TagsLink }}">Tags</a> |<a href="{{ .RSSLink }}">RSS</a> | &copy; {{ .Copyright }} | Built with <a href="https://github.com/harrisonpage/draft">Draft</a> Version {{ .Version }}
    </footer>
</body>
</html>
